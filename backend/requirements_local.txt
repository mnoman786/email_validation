# Local development requirements (Windows, Python 3.9+, no Docker)
# Django 4.2 LTS — compatible with Python 3.9

Django==4.2.20
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.3.1
django-filter==24.2
django-extensions==3.2.3

# Celery (runs synchronously in local mode - no broker needed)
celery==5.3.6
kombu==5.3.4

# Email validation
dnspython==2.6.1
email-validator==2.1.1

# HTTP
requests==2.32.3

# Stripe
stripe==10.6.0

# CSV
pandas==2.2.2

# Auth
PyJWT==2.8.0

# API Docs
drf-spectacular==0.27.2

# Environment
python-decouple==3.8

# Celery results (uses DB, no Redis needed)
django-celery-results==2.5.1
django-celery-beat==2.6.0

# WSGI server (used by Ubuntu/systemd deployment)
gunicorn==22.0.0

# Static files
whitenoise==6.7.0

# Images
Pillow==10.3.0
